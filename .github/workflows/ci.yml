name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      # --- NEW STEP: Generate Astro Types ---
      - name: Sync Astro Types
        run: npx astro sync

      - name: Check TypeScript
        run: npx typescript --noEmit

      - name: Build the Astro Site
        run: npm run build

      # --- NEW DEPLOYMENT STEP ---
      - name: 📂 Sync files to Hostinger
        # This action only runs if the branch is 'main'
        if: github.ref == 'refs/heads/main'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          # We only want to upload the final, compiled static files
          local-dir: ./dist/ 
          # Ensure it deploys to the public_html directory on Hostinger
          server-dir: /public_html/

